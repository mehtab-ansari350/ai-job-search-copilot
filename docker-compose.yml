version: "3.9"

services:
  ai-job-search-copilot:

    build: .

    container_name: ai-job-search-copilot

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./database:/app/database